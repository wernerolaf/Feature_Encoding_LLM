# Feature_Encoding_LLM

## Testing
- `PYTHONPATH=$(pwd) python -m scripts.prepare_autoencoders --help` (verify autoencoder CLI loads)
- `PYTHONPATH=$(pwd) python -m scripts.train_probes --help` (verify probe training CLI loads)
